
const service = (state = [{
  "id": "easylearn-user",
  "endpointCount": 4,
  "serviceCallCount": 1
}], action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default service;

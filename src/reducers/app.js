const init = {
  sidebar: {
    showEndpoint: false,
    showServiceCall: false,
    showService: false,
    serviceId: 'easylearn-user',
  }
}
const app = (state = init, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default app;
